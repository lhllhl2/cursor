<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jasolar.mis.module.system.mapper.admin.org.SystemOrgMapper">



    <select id="pageForMapper" resultType="com.jasolar.mis.module.system.resp.SystemOrgResp">

        select
            o.id,
            o.org_name,
            o.parent_id,
            og.org_name parent_name
        from system_org o
        left join system_org og on o.parent_id = og.id and o.deleted = 0 and og.deleted = 0
    </select>
</mapper>