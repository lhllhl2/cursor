# cursor

本地 / Cloud Agent **全栈（Gateway + System + Oracle + Redis + JA_FSMS_FE）** 初始化说明见：

**[docs/LOCAL_FULLSTACK_SETUP.md](docs/LOCAL_FULLSTACK_SETUP.md)**

快速构建后端 JAR 并打印后续步骤：

```bash
./scripts/bootstrap-fullstack.sh --build
```

环境自检（不构建）：

```bash
./scripts/bootstrap-fullstack.sh --check
```
