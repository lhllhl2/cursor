## Cursor Cloud specific instructions

This is a minimal starter repository. As of initial setup, it contains only a `README.md`.

- **No application code, dependencies, or services** exist yet.
- **No build/lint/test commands** are configured.
- When application code is added, update this section with relevant dev commands, service startup instructions, and testing notes.
